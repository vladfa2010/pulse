/**
 * =============================================================================
 * PULSE — Конфиг реалтайм-кластеризации новостей (ТЗ-92)
 * =============================================================================
 *
 * Пороги откалиброваны по calibration_pairs.csv (ТЗ-91): 3607 позитивных пар
 * из импортированных кластеров + 8277 негативных. Максимум негативов 0.743,
 * медиана дублей 0.80. Пороги вынесены в конфиг именно потому, что могут
 * дрейфовать на живом потоке — корректировка конфигом, без переделки кода
 * (риски §4 ТЗ-92).
 */

/** Порог авто-склейки: sim >= T1 → приклеить без LLM */
export const CLUSTER_T1 = 0.80;
/** Порог зоны сомнений: T2 <= sim < T1 → LLM-верификатор */
export const CLUSTER_T2 = 0.55;

/** Окно жизни кластера, часов: приклеиваем только если last_seen_at кластера
 *  в пределах GAP_H от published_at новости (защита от недельных мега-кластеров) */
export const CLUSTER_GAP_HOURS = 36;

/** Окно поиска кандидатов назад от новости, часов */
export const CANDIDATE_WINDOW_HOURS = 48;

/** Суточный потолок вызовов LLM-верификатора (страховка от взрыва стоимости;
 *  при превышении пары зоны сомнений отклоняются) */
export const VERIFIER_DAILY_LIMIT = 2000;

/** Рубрик-черный список (Методология §4, референс _cascades_full.py:35-37):
 *  заголовки-дайджесты не участвуют в склейке ни сидом, ни дублём */
export const RUBRIC_BLACKLIST = [
  'что случилось этой ночью',
  'что произошло за день',
  'главное к этому часу',
  'цб установил официальные курсы',
  'курсы валют на',
  'итоги торгов',
  'новости дня',
  'дайджест',
  'главные новости',
];

/** Верификатор: критерий совпадения — факт-событие, а не тема */
export const VERIFIER_MODEL_TEMPERATURE = 0.6;
export const VERIFIER_TIMEOUT_MS = 60_000;
export const VERIFIER_MAX_TOKENS = 512;
