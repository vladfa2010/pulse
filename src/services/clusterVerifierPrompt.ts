/**
 * =============================================================================
 * PULSE — Промпт LLM-верификатора пар (ТЗ-92, задача 3)
 * =============================================================================
 *
 * Текст — проект из Методологии §9.4 (критерий и контрпримеры — из §1).
 * Критерий «та же новость» — совпадение факта-события, а не темы.
 * Числовое вето до зон уже отсеяло пары с разными значимыми числами;
 * верификатор решает хвост парафраза в зоне T2..T1.
 *
 * Ключ ответа — `same_fact` (как в §9.4). Парсинг по explicit keys,
 * fail-closed (Методология §9.3).
 *
 * Файл вынесен отдельно по ТЗ-92 (задача 3): «промпт приложить в коммит
 * отдельным файлом».
 */

export const CLUSTER_VERIFIER_PROMPT_TEMPLATE = `Ты — редактор новостной ленты PULSE. Определи, сообщают ли две новости ОДИН И ТОТ ЖЕ ключевой факт.

Новость A: «{{TITLE_A}}» ({{SOURCE_A}}, {{PUBLISHED_A}})
Новость B: «{{TITLE_B}}» ({{SOURCE_B}}, {{PUBLISHED_B}})
{{SUMMARY_A_BLOCK}}
Дубль = тот же факт без существенно новой информации.
НЕ дубли (контрпримеры):
- одно событие, разные новости («Сбер отчитался» ≠ «Аналитики про отчёт Сбера»);
- один брифинг, разные тезисы;
- шаблонные сводки с разными числами («85 дронов» ≠ «516 дронов»);
- развитие истории (закрытие консульства → ответ МИД — разные факты);
- рыночные вехи с разными значениями («Brent > $90» ≠ «Brent > $94»).

Ответь строго JSON:
{"same_fact": true|false, "confidence": 0.0-1.0, "reason": "1 предложение на русском"}`;
